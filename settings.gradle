import org.gradle.api.initialization.resolve.RepositoriesMode

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
//        this code below is PAIN IN THE ASS! REMEMBER IT!
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
rootProject.name = "Instagreat"
include ':app'
